import jQuery from "jquery";
window.$ = window.jQuery = jQuery;
"use strict";
document.addEventListener("DOMContentLoaded", function() {
    // document.getElementById("customers-comments").querySelectorAll("li").forEach(function(t) {
    //     for (var e = t.querySelector("header h4"), i = parseInt(t.dataset.rate / 2), s = t.dataset.rate % 2, a = "", n = i; 0 < n; n--)
    //         a += "★";
    //     s && (a += "*");
    //     for (var o = 5 - i - 1; 0 < o; o--)
    //         a += "☆";
    //     e.insertAdjacentHTML("beforeend", '<span class="rate">' + a + "</span>")
    // })
}),
$(function() {
    function t(t) {
        $(t).each(function() {
            // $(this).find("ul li:first").addClass("show"),
            // $(this).prepend('<img src="data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" class="lazyload" alt="">');
            // var t = $(this).find(".show img").data("src");
            // $(this).find("> img").attr("data-src", t)
        }),
        $("ul li", t).on({
            // mouseenter: function() {
            //     $(this).parents("ul").find("li").removeClass("show"),
            //     $(this).addClass("show");
            //     var t = $(this).find("img").data("src")
            //       , e = $(this).parents("figure").find("> img").outerHeight(!0);
            //     $(this).parents("figure").find("> img").remove(),
            //     $(this).parents("figure").prepend('<img src="' + t + '" style="opacity:0" alt="">'),
            //     $(this).parents("figure").find("> img").css("height", e).animate({
            //         opacity: "1"
            //     }, 1e3)
            // }
        })
    }
    var i;
    t("#item-images"),
    t("#items > section > figure"),
    $("#item-detail details").one("click", function(t) {
        $("> *", this).not("summary").wrapAll("<div />")
    }),
    $("#item-detail details").on("click", function(t) {
        if (null == $(this).attr("open")) {
            $(this).attr("open", "");
            var e = $("> div", this).outerHeight(!0);
            $(this).attr("open", null),
            $("> div", this).css("height", 0),
            $("> div", this).stop().animate({
                height: e
            }, 500)
        } else {
            var i = $(this);
            $.when($(this).attr("open", null), $("> div", this).stop().animate({
                height: 0
            }, 500)).done(function() {
                $("> div", i).removeAttr("style"),
                $(i).removeAttr("open")
            }).fail(function() {
                console.log("error")
            })
        }
    }),
    i = "#BeautyComponents > figure li",
    $(i).css("opacity", 0),
    $(i).on("inview", function(t, e) {
        e ? $(i).each(function(t) {
            $(this).delay(500 * t).queue(function() {
                $(this).stop().addClass("show").dequeue()
            })
        }) : $(this).stop().removeClass("show")
    })
}),
$(document).ready(function() {
    var e = "#item-buy"
      , t = "#gotoItem";
    if ($(e).length) {
        var i = $("html").attr("lang")
          , s = "<p " + (t.replace("#", 'id="') + '"') + '><a href="' + e + '">' + {
            ja: "購入する",
            en: "Go to Item",
            "zh-tw": "購買",
            "zh-hk": "購買",
            "zh-cn": "购买",
            "zh-TW": "購買",
            "zh-CN": "购买"
        }[i] + "</a></p>";
        $("main").append(s),
        $(t).click(function() {
            var t = $(e).offset().top;
            return $("body, html").animate({
                scrollTop: t - 110
            }, 500),
            !1
        })
    }
});
//# sourceMappingURL=product.min.js.map
